def remove_char(s):
    # this is a short notation for slicing
    # items start through stop are returned as in return
    # just whats in between 1st char and last char
    return s[1:-1]


print(remove_char('Cheese'))